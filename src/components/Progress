function Progress({index + 1} numQuestions={numQuestions}) {
  return (
    <header className="progress">
      <p>Question <strong>(index)</strong> / {numQuestions}</p>

      <p><strong>{points}</strong></p>
    </header>
  );
}

export default Progress;